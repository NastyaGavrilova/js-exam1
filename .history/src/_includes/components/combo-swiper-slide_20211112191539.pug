mixin combo-swiper-slide(args)
  -let src = args.src;
  -let price = args.price;
  -let lastPrice = args.lastPrice;
  -let title = args.title;
  -let description = args.description; 
  -let type = args.type;
  -let content = args.content;
  -let contentName = args.contentName;
  -let classList = args.classList;
  -let className = args.className;
  -let spanClassName = args.spanClassName;
  -let spanClassList = args.spanClassList;
  -let id = args.id;
  -let idName = args.idName;
    .c-combo-swiper-slide
      .c-combo-swiper-slide__wrapper 
        .c-combo-swiper-slide__img 
          img(src="" alt="")