mixin contacts()
  .c-contacts 
    .c-contacts__wrapper
      .c-contacts__title 
        +title({
          classList:"e-title--montserrat",
          content:"CONTACTS",
        })
      .c-contacts__left 
        form.c-contacts__form(action="#" method = "GET")
